import { type Project } from 'ts-morph';
export declare function updateNodeAst({ nodePath, className, baseUrl, project, }: {
    nodePath: string;
    className: string;
    baseUrl: string;
    project?: Project;
}): import("ts-morph").SourceFile;
export declare function updateCredentialAst({ repoName, baseUrl, credentialPath, credentialName, credentialDisplayName, credentialClassName, project, }: {
    repoName: string;
    credentialPath: string;
    credentialName: string;
    credentialDisplayName: string;
    credentialClassName: string;
    baseUrl: string;
    project?: Project;
}): import("ts-morph").SourceFile;
export declare function addCredentialToNode({ nodePath, credentialName, project, }: {
    nodePath: string;
    credentialName: string;
    project?: Project;
}): import("ts-morph").SourceFile;
